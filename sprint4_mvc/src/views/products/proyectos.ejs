<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
   Lista de proyectos

   <% products.forEach(product => { %>
    <li class="product">
        <a href="/proyectos/<%= product.id %>">
            <% if (product.image) { %>       
                <img src="/uploads/<%= product.image.url %>" alt="" srcset="">
            <% } %>
            <span>
            <%= product.name %>
            </span>
            <span>
            $<%= product.precio %>
            </span>
            <a href="/proyectos/update/<%= product.id %>">
                update
            </a>
            <form action="/proyectos/?m=DELETE" method="post">
                <input type="hidden" name="id" value="<%= product.id %>">
                <button type="submit"> Eliminar </button>
            </form>
        </a>
    </li>
    <%})%>

</body>
</html>